source "https://rubygems.org"

gemspec
spec.add_runtime_dependency 'dentaku'

gem 'dentaku', '~> 3.5.1' # needed for calculating the width based on SAR

group :test do
  gem 'webmock'
  gem 'simplecov'
end
